apiVersion: v1
kind: Service
metadata:
  name: maumau
  labels:
    app: maumau
spec:
  type: ClusterIP
  selector:
    app: maumau
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
