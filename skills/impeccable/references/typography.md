## Typography

- Pick fonts from the brand voice, not from habit.
- Avoid default monoculture choices when the brief needs character.
- Use fewer sizes with stronger hierarchy instead of many nearly-identical sizes.
- Keep body copy near `65-75ch` max width.
- Use tighter tracking only when it improves the specific typeface.
- For product UI, prefer stable type scales over oversized hero-style swings.
