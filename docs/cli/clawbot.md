---
summary: "CLI reference for `maumau clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `maumau clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `maumau clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `maumau clawbot qr` (same behavior as [`maumau qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `maumau clawbot qr` -> `maumau qr`
